# Plan: ADT-Bench — AI-driven ABAP/MCP Comparison Framework

## Goal (recap)

An SDD + TDD-first, AI-first, open-source benchmark arena that runs the **same agent loop** (mastracode / @mastra/core) against the **same ABAP scenarios** (markdown Given/When/Then) using **three different tool surfaces** as the only variable:

1. **`abapify/adt-cli`** (TypeScript, npm) — CLI exposed via `npx @abapify/adt-cli` + skills
2. **`marianfoo/arc-1`** (TypeScript, MCP) — stdio MCP server
3. **`marcellourbani/vscode_abap_remote_fs`** (TypeScript, embedded MCP) — same author's stack as `abap-adt-api`

Captured per (scenario × tool × run): pass/fail, tool-call count, tokens in/out, wall time, USD cost, #steps. JSON output. v1: console. v2: static website.

All runs hit a **mock ADT server** (no BTP key needed in CI). Same swap point accepts a real BTP service key in production.

---

## Constraints honored (from user)

- **TypeScript** everywhere — drop Inspect (Python). Roll our own lightweight harness, or use a TS-native framework.
- **Nx monorepo** — single repo, multiple packages, shared TS config, shared types.
- **`mastracode`** as the agent — its `createMastraCode()` is embeddable, headless, instrumented, hook-able.
- **Skills can be rebuilt locally** — no upstream dependency on `abapify/skills` in v1. We author them inside the bench repo; later migrate to shared.
- **3 tools** in v1: `abapify/adt-cli`, `arc-1`, `vscode_abap_remote_fs`.
- **Mock ADT server** first; BTP key swap later via `BTP_SERVICE_KEY` env.
- **No autonomous PRs to public upstream repos.** All work goes into a **new repo** under the `abapify` org. Drafting a PR to `arc-1` is a separate, explicit future request.
- **SDD / TDD / AI-first**: specs before code, tests before impl, agent-first design.

---

## Architecture

```
adt-bench/  (nx monorepo, TypeScript, Apache-2.0)
├── packages/
│   ├── mock-adt-server/        # Express mock of SAP ADT REST API
│   ├── scenarios/             # Markdown Given/When/Then + Zod schema + fixtures
│   ├── harness/               # Headless agent loop, metrics capture, eval runner
│   ├── tools-adt-cli/         # Adapter: turns adt-cli into Mastra tools
│   ├── tools-arc-1/           # Adapter: stdio MCP client → Mastra tools
│   ├── tools-abapfs/          # Adapter: vscode_abap_remote_fs MCP → Mastra tools
│   ├── skills/                # Benchmark-specific agent skills (markdown + scripts)
│   ├── eval/                  # Scenario runner, results aggregation, JSON report
│   └── web/                   # (v2) Static site that renders the JSON
├── scenarios/                 # Markdown files (one per task)
├── fixtures/                  # Pre-seeded mock ADT data
├── results/                   # JSON output (gitignored except .gitkeep)
├── docs/
│   ├── design.md              # This plan, expanded
│   ├── metrics-schema.md      # Exact JSON contract for results
│   ├── mock-adt-spec.md       # ADT endpoint catalogue + curl examples
│   └── reproducing.md         # How to run, how to add a scenario
├── .github/workflows/ci.yml   # Lint + typecheck + test + benchmark on PR
├── nx.json
├── package.json               # pnpm workspaces
└── README.md
```

### Key design decisions (rationalized)

| Decision | Choice | Why |
|---|---|---|
| Monorepo tool | **Nx 20+** with pnpm workspaces | User-requested. Nx gives us task graph, affected-only CI, generators. |
| Agent loop | **`createMastraCode()` from `mastracode`** | User-requested. Embeddable, hook-able, LibSQL token storage, YOLO mode, `@mastra/mcp` first-class. |
| MCP client | **`@mastra/mcp`** (Mastra's MCP client) | Already bundled; same loop handles all 3 tools uniformly. |
| CLI invocation | **`execa`** in a Mastra "bash-style" tool wrapper | `adt-cli` commands are subcommands — we generate one Mastra tool per command. |
| Mock ADT server | **Express 4 + xmlbuilder2** (TS) | Same language as everything else; matches the research spec. |
| Scenarios | **Markdown files + Zod schema in `scenarios.ts`** | Given/When/Then is markdown. Validation is Zod. |
| Metrics | **Custom collector + Mastra hooks** | Tokens from Mastra. Tool calls from hook. Wall time from `performance.now()`. |
| Results | **JSONL append-only, one file per run, committed to a `results/` branch** | Trivial to render; git-diffable. |
| Testing | **Vitest** (unit + integration) + **`@microsoft/tui-test`** if needed | TS-native, fast, Nx-friendly. |
| Linting | **ESLint strict + tsgo (typecheck)** | Project default. |
| Spec/SDD | **`docs/specs/*.md` per package, required-before-code** | Gate. `pnpm spec-check` fails if a package's `docs/specs/*.md` is older than its `src/`. |

### Data flow (per run)

```
[ scenario.md ]  ─┐
[ tools/MCP-X  ] ─┼─> harness.run(scenario, toolBundle)
[ LLM provider  ] ─┘            │
                                ▼
                  Mastra agent loop (hook on every step)
                                │
                  ┌─────────────┼─────────────┐
                  ▼             ▼             ▼
              tool_calls   tokens/cost    wall_time
                  │             │             │
                  └─────────────┴─────────────┘
                                │
                                ▼
                  results/<run-id>.json  (NDJSON append)
                                │
                                ▼
                  pnpm eval:report  →  results/summary.json  →  (v2) static site
```

### Metrics JSON contract (per run)

```ts
type RunResult = {
  run_id: string;                  // ulid
  started_at: string;              // ISO
  finished_at: string;             // ISO
  duration_ms: number;
  scenario: { id: string; title: string; difficulty: 'easy'|'medium'|'hard' };
  tool_under_test: { id: 'adt-cli'|'arc-1'|'abapfs'; version: string; transport: 'cli'|'mcp-stdio' };
  llm: { provider: string; model: string };
  outcome: 'pass'|'fail'|'timeout'|'budget_exceeded'|'error';
  score: { primary: number; details: Record<string, number> };  // 0..1
  metrics: {
    tool_calls: { total: number; by_tool: Record<string, number> };
    tokens:     { input: number; output: number; cache_read: number; cache_write: number };
    cost_usd:   number;
    steps:      number;            // LLM turns
    bytes_in:   number;            // sum of tool input sizes
    bytes_out:  number;
  };
  transcript_path: string;         // .eval-style JSON, full message log
  errors: string[];
};
```

---

## Scenarios (v1, 6 tasks)

Each is a markdown file `scenarios/<id>.md` with `## Given / ## When / ## Then` + Zod-validated metadata in frontmatter.

| ID | Title | Difficulty | Validates |
|---|---|---|---|
| `create-class-hello` | Create `ZCL_BENCH_HELLO` with `say_hello( )->string` returning `'Hello, world!'`, activate, run AUnit. | easy | create, write, activate, aunit, CSRF, lock/unlock |
| `read-class-source` | Find the existing `ZCL_BENCH_FIXTURE_OK`, read its source, print the class name. | easy | search, read |
| `syntax-error-recovery` | Write invalid ABAP, run checkruns, interpret the error, fix it, re-check. | medium | checkruns error parsing, self-correction |
| `abapunit-failure-diagnose` | Read `ZCL_BENCH_FIXTURE_FAIL_AUNIT`, run AUnit, parse the JUnit result, identify the failing assertion. | medium | aunit, junit XML parse |
| `create-transport-zpkg` | Create class in `ZPKG_BENCH` package, run transportchecks, link the created TR. | medium | transportchecks, transport awareness |
| `bdef-scaffold-rap` | Create a behavior definition for `ZCL_BENCH_HELLO` with `for create` and `for update delete`. | hard | BDEF namespace, RAP preflight |

Each scenario has a matching `fixtures/<id>.json` pre-seeding the mock server.

---

## Skills (local, rebuildable)

Authored under `packages/skills/.agents/skills/<name>/SKILL.md`, agentskills.io format. 4 skills in v1:

- `abap-workflow` — teaches the agent the standard lock → write → unlock → activate order, CSRF bootstrap, error-recovery patterns.
- `abap-test-loop` — when the task is "make tests pass", the canonical cycle (read source → write → checkrun → aunit → parse junit → fix).
- `abap-mock-usage` — when the mock server is in use, quirks to expect (`!@MOCK_AUNIT_FAIL` markers, deterministic runIds, no real transport).
- `abap-eval-grader` — used by the **evaluator agent** (second agent) to score a run; reads the transcript, walks the scenario's Then clauses, emits a per-clause pass/fail.

All four are loaded by the harness as `system_message` fragments into the Mastra agent at start. They are tool-agnostic by design — they describe the *work*, not the *API*.

---

## Mock ADT Server (`packages/mock-adt-server`)

Implements the 16-endpoint minimum set from the research spec (mock_adt_spec.md):
- discovery, search (RIS quickSearch GET), class CRUD, lock/unlock, activation, checkruns, abapunit (start/poll/results), transportchecks, packages
- CSRF token flow (`X-CSRF-Token: Fetch` on GET → token in response; required on POST/PUT)
- Fixtures loader: reads `fixtures/*.json` on boot, exposes `/__mock/reset` (admin-key gated)
- Determinism knobs: `X-Mock-Delay-Ms` header, `X-Mock-Aunit-Outcome: pass|fail` header for fine-grained control
- Content types match SAP exactly: `application/vnd.sap.adt.oo.classes.v2+xml`, `application/vnd.sap.adt.checkmessages+xml`, `application/vnd.sap.adt.api.junit.run-result.v1+xml`, etc.
- All `/__mock/...` admin routes return JSON; all ADT routes return SAP-shaped XML

Tested with **Vitest + supertest** + a captured-response fixture set from real SAP (downloaded from a public `sapse/abap-adt` sample repo, see research).

---

## Tool Adapters

### `packages/tools-adt-cli` (CLI + skills)
- Uses `execa` to spawn `npx @abapify/adt-cli`
- Generates **one Mastra tool per adt-cli subcommand** at startup by running `adt --help` and parsing
- Each tool: `execute(args) -> string` — runs the command, captures stdout/stderr/exit code
- Auth via `BTP_SERVICE_KEY` env or the mock's Basic auth (`--user demo --password demo --url http://localhost:8080`)
- Skills `abap-workflow`, `abap-test-loop` loaded as system_message

### `packages/tools-arc-1` (MCP stdio)
- Uses `@mastra/mcp` to spawn `npx arc-1@latest --url <mock> --user demo --password demo --stdio`
- MCP tool list auto-discovered; one Mastra tool per MCP tool, 1:1 mapping
- Skills same as CLI variant

### `packages/tools-abapfs` (MCP from vscode extension)
- Same pattern as `tools-arc-1`, but the MCP server is the embedded one in `vscode_abap_remote_fs`
- **Caveat (will validate early):** this MCP requires a running VS Code instance. If true, we add a stub-mode fallback that uses `abap-adt-api` directly via `tsx` and exposes the same 40-tool surface as MCP — that lets us compare apples-to-apples without needing a UI host. Decision point in the v1 sprint: confirm whether we run VS Code headless via `xvfb` + `code --headless` (vibing-steampunk does this) or take the stub-route.
- Skills same

All three adapter packages have the **same exported TS interface**:
```ts
export interface ToolBundle {
  id: 'adt-cli' | 'arc-1' | 'abapfs';
  version: string;            // resolved at runtime
  transport: 'cli' | 'mcp-stdio';
  tools: Tool[];              // Mastra ToolDef[]
  systemFragments: string[];  // skill markdown rendered to system message
}
```

---

## Harness (`packages/harness`)

A thin TypeScript module. ~400 LoC. No framework beyond Mastra.

```ts
// harness.run
export async function run(args: {
  scenario: Scenario;
  bundle: ToolBundle;
  llm: { provider: string; model: string; apiKey: string };
  limits: { maxSteps: number; maxTokens: number; maxCostUsd: number; timeoutMs: number };
  mockBaseUrl: string;       // http://127.0.0.1:8080
}): Promise<RunResult>
```

- Spawns/connects tools, injects skills, runs Mastra agent, subscribes to hooks:
  - `onStepFinish` → increment `steps`, capture last tool call
  - `onToolCall` → tally by tool name
  - `onUsage` → accumulate tokens
- Wall time around the whole `run()`
- Persists transcript to `results/<run-id>/transcript.json`
- Returns `RunResult`
- Exits cleanly on limits (counts as `timeout`/`budget_exceeded`)

### CLI

```bash
pnpm bench:run --tool adt-cli --scenario create-class-hello --model anthropic/claude-sonnet-4-5
pnpm bench:run --all                           # full matrix: 3 tools × 6 scenarios × N trials
pnpm bench:report                              # aggregate results/ → results/summary.json
pnpm bench:serve                               # (v2) static site on :4000
```

### Reproducibility

- `--seed` for LLM temperature=0 and per-run deterministic seeds
- `--trials N` runs each (tool, scenario) N times; aggregates mean/median/stdev
- `--parallel K` runs K concurrent (default: 1 for fairness on tokens; K for wall-time)

---

## SDD/TDD Gates

Enforced in `pnpm verify`:

1. **`pnpm spec-check`** — for each package, `docs/specs/<pkg>.md` must have a newer mtime than the latest `src/**` change. Fails CI otherwise. Generated by `pnpm spec:touch` after editing a spec.
2. **`pnpm test`** — Vitest must pass.
3. **`pnpm typecheck`** — `tsc --noEmit` clean.
4. **`pnpm lint`** — ESLint clean.
5. **`pnpm mock:test`** — mock-adt-server passes its captured-response tests.
6. **`pnpm bench:smoke`** — runs one scenario with one tool against the mock to prove end-to-end wiring.
7. **PR template** requires a changeset (`pnpm changeset`) following the changesets pattern.

`pnpm verify` is the single gate. PRs can't merge without it.

---

## Repository setup

- New repo: **`abapify/adt-bench`** (Apache-2.0).
- Created via `gh repo create abapify/adt-bench --public --description "AI-driven benchmark arena for ABAP ADT MCP servers and CLIs"` using `GH_TOKEN` already available in the environment.
- Local clone in the current session workdir.
- Initial commit = Nx workspace skeleton + this plan.
- CI: GitHub Actions, Node 22 matrix, pnpm cache, `pnpm verify` on PR, `pnpm bench:smoke` on push to main (results uploaded as artifact).

### No upstream PRs in v1

Drafting a PR against `marianfoo/arc-1` (proposing they ship a `--mock` flag) is **out of scope** for this session. Will be a separate, explicit ask. Same for `abapify/adt-cli` (propose they add `--output json` to all commands).

---

## Session deliverables (what gets built THIS session)

Strict, scoped, TDD:

1. **Repo skeleton** — Nx workspace, pnpm, TS strict, ESLint, Vitest, GitHub Actions. ~30 min.
2. **`docs/design.md`** (this plan) + **`docs/mock-adt-spec.md`** + **`docs/metrics-schema.md`** — written first. SDD gate. ~30 min.
3. **`packages/mock-adt-server`** with the 16-endpoint minimum + Vitest tests (TDD: red → green) using captured-response fixtures. ~2 h.
4. **`packages/scenarios`** — 2 of the 6 scenarios (`create-class-hello`, `read-class-source`) with Zod-validated markdown. ~30 min.
5. **`packages/harness`** with the run/eval/JSON-output core. ~1 h.
6. **One tool adapter** (`tools-arc-1` — the most-mature MCP), tested with the harness against the mock. ~1 h.
7. **Smoke benchmark run** — `pnpm bench:smoke` produces a real `results/summary.json`. ~30 min.
8. **README** + **CONTRIBUTING** + first changeset. ~20 min.

**Total: ~6 hours of work, all in this session, all TDD, all green CI, smoke run produces real JSON.**

**Explicitly NOT in this session:**
- The other 2 tool adapters (v1.1)
- The other 4 scenarios (v1.1)
- The static website (v2)
- Any PRs to upstream repos
- The `abapify/skills` shared skills repo
- BTP live integration (mock only)

---

## Risks & mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| `vscode_abap_remote_fs` MCP needs VS Code host | high | medium | Stub-mode using `abap-adt-api` directly; v1.1 task explicitly addresses |
| `mastracode` 0.22 API changes | medium | medium | Pin version; interface boundary in `harness` is the abstraction |
| arc-1 install requires license / specific BTP creds | low | low | Runs against mock — no creds needed; `--url http://127.0.0.1:8080` |
| ADT XML namespaces drift across SAP versions | low | low | Lock the namespace map to v2/v1; document mismatches as known issues |
| LLM nondeterminism ruins reproducibility | medium | medium | Temperature 0, per-run seed, multiple trials, report std |
| Nx + pnpm + TS path aliases fighting | medium | low | Use Nx's `tsconfig.base.json` extension once; do not fight it |
| CI cost (running 3 LLMs × 6 scenarios × N trials) | medium | medium | v1 = smoke only in CI; full matrix runs on release tags or manually |

---

## Open questions for user (please confirm or override)

1. **Repo name:** `abapify/adt-bench` — OK? Alternative: `abapify/adk-bench`, `abapify/adt-arena`.
2. **`vscode_abap_remote_fs` stub-mode OK?** — use `abap-adt-api` directly when the embedded MCP needs a VS Code host, in v1.1.
3. **First two scenarios:** `create-class-hello` + `read-class-source`. OK or swap in `bdef-scaffold-rap` for a more visible demo?
4. **Smoke run uses `claude-sonnet-4-5` via env `LLM_API_KEY`** (we have one in the sandbox per the GH_TOKEN pattern). OK, or use a local Ollama default?
5. **Don't open any upstream PRs this session** — confirmed by your "we decouple later". Locking that in.
6. **No website in v1** — `results/summary.json` is the deliverable, console `pnpm bench:report` prints a table. OK?
